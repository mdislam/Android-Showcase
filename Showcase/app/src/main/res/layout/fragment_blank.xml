<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             xmlns:tools="http://schemas.android.com/tools"
             android:layout_width="match_parent"
             android:layout_height="match_parent"
             android:background="@color/goly_orange"
             tools:context="org.mesba.showcase.fragments.BlankFragment">

    <!-- TODO: Update blank fragment layout -->
    <TextView
        android:padding="20dp"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="@string/hello_blank_fragment"
        android:textSize="26sp"
        android:textColor="@color/goly_white"
        android:textStyle="bold"/>

    <Button
        android:id="@+id/btn_fetch_data"
        android:layout_width="wrap_content"
        android:layout_height="60dp"
        android:background="@android:color/transparent"
        android:layout_margin="100dp"
        android:drawableTop="@drawable/ic_wifi_placer"
        android:text="WiFi Location"/>

</FrameLayout>
